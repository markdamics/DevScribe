# Missing features

1. scrollbar at tabbar
2. variable, functions names highlightink in the canvas if the cursor it's on the object.
3. mouse change to pointer when the mouse is on an object with ctrl pressed down
